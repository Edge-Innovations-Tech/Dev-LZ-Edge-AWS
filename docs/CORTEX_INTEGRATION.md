# Cortex Integration

Each module emits a `cortex_inventory` output for catalog registration and downstream automation.

Required tags:

- `cortex:environment`
- `cortex:owner`
- `cortex:cost_center`
- `cortex:data_classification`

The catalog manifest is stored at `cortex/catalog-manifest.yaml`.
